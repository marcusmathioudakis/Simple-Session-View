{
  "name": "grid-sampler",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://marcusmathioudakis.github.io/grid-sampler",
  "dependencies": {
    "gh-pages": "^1.0.0",
    "rc-slider": "^8.3.5",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.16",
    "tone": "^0.11.11"
  },
  "scripts": {
    "start": "cross-env NODE_PATH=src/scripts:src/styles react-scripts start",
    "build": "cross-env NODE_PATH=src/scripts:src/styles react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "cross-env": "^5.1.1"
  }
}
